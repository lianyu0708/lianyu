# Render 一键部署蓝图（免费版）
# 手机浏览器打开：https://render.com/deploy?repo=https://github.com/lianyu0708/lianyu
services:
  - type: web
    name: lianyu
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
